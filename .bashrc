# executed automatically for all non-login bash shells (when you explicitly type "bash" on the command line or run a script that starts with #!/bin/bash).

# Source bash profile
[ -n "$PS1" ] && source ~/.bash_profile;
